class FlashcardSet {
    constructor(author, topic, flashcards) {
        this.author = author;
        this.topic = topic;
        this.flashcards = flashcards;
    }
}

module.exports = FlashcardSet;